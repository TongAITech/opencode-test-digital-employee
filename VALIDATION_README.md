# AITest V1.13.0 · Windows 验证

将 ZIP 完整解压。打开 Windows x64 Git Bash，在解压目录执行以下示例，把父目录和工作目录名称换成您自己的位置：

```bash
./INSTALL.sh /d/Testing BLOAN
cd /d/Testing/BLOAN
./AITEST.sh
```

安装无需管理员权限或联网。依赖随包安装，安装时不启动 OpenCode、不要求模型或行内授权。已有工作目录不会被覆盖。以后始终在这个工作目录运行 `./AITEST.sh`。

选择“开始/继续测试对话”，直接使用 Git Bash 中已有的宿主 `opencode` 及其原有模型和登录配置。本产品不复制宿主配置或凭据。宿主 OpenCode 尚未安装或模型不可用时，按行内 OpenCode 的正常流程准备好后再启动。

进入对话后输入：

```text
测试 BLOAN-PF1.1.0
```

需要附件时，在菜单选择“导入需求/SST 附件”，选中任务和 DOCX、PDF、TXT、MD 或 JSON 文件，再返回对话输入“继续测试”。任务和进度会保存在本工作目录，重新启动可继续。

首次使用测试环境，选择“绑定测试环境”，填写已批准的环境地址、HTTP 方法和批准记录。4A 使用“浏览器人工教学 / 4A”绑定受控浏览器；密码、验证码只在浏览器中输入。任务要求人工操作时，完成后回到对话输入“完成”。系统重新检查同一浏览器的登录、页面和业务状态，通过后自动继续原步骤。文字“完成”本身不代表登录成功。

Starlink 暂时不可连接时，选择“导入 Current Release / Starlink 批准导出”，填写项目、版本、批准编号和有效期。导出需包含项目、发布版本、需求与仓库列表、修订号和采集时间。导入成功只证明批准文件已接入。

CAT、DB、TDSQL、增量覆盖平台由行内负责人完成适配器和授权绑定。缺失时显示 `BANK_BINDING_REQUIRED`。凭据交给行内登录或凭据管理入口，不放入对话和附件。

人工教学完成后，可在菜单选择“教学候选回放”，核对操作并提供批准记录，在已准备好的受控测试页面实际回放。选择“审核任务知识”可将核对过且有执行证据的候选纳入有效期内的任务检索。两者均不会自动提升为全局技能。

选择“导出证据”获取 `data/exports` 下的证据 ZIP。机器验证结果和行内验证分别记录；本地合成服务的通过不代表 BLOAN、4A、Starlink、CAT 或 DB 的行内通过。
