# PFC V3.2 launch and AI Runtime contract

START only admits the bank-proven Git Bash `opencode` command, package-owned workspace, valid positive CLI port, live process, listener and HTTP-ready Web. It prints the process result and returns without waiting for authentication.

AI Runtime is separate: Auth → Provider/Model → real LLM → R2 session. Each later state may remain waiting while the same Web stays usable. `STATUS` probes the same instance; authentication changes do not force a restart and normal auth paths never invoke `taskkill`. PFC Mission continuation requires `PFC_MISSION_AI_READY`; Coverage/Cases/R3 and real execution remain HOLD.
