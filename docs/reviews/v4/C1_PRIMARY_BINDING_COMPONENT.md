# C1 Primary binding increment

Target 1.14.0 CANDIDATE / NOT_QUALIFIED. Architecture v7 unchanged. This is a prerequisite component; C1 full, C2 automatic pressure rotation and final installed product qualification remain OPEN.

PRIMARY_INTERACTION is an additive R1 typed root owned by primary_interaction_sessions 1.0.0. Its trusted launcher owns CREATE_PRIMARY_INTERACTION and RECORD_PRIMARY_BINDING; model tools expose neither. Events have null core session_id and create no Mission or core Session. Stored identity binds resolved workspace, logical Director, monotonic epoch, deterministic provision token, actual Host Session, eight-hour lease and a hash of the trusted provider endpoint plus workspace. Endpoints identify the local OpenCode service, not the model provider. Endpoint/port changes conservatively fence the predecessor, including incomplete provisioning, and allocate a new epoch. This does not claim a stable server instance across endpoint reuse or transparent process restart.

REQUEST precedes Host creation. A retry locates the exact provision title only in the recorded Host realm; BIND uses observed directory identity. FENCE rejects prior bindings. Old owner compositions reject unknown-root writes and rebuilds using existing CC01 compatibility; canonical Mission event format is unchanged. Replay applies the same bounded schema checks as commands.

The Windows launcher now obtains its Primary from R1. The old operational pointer is never authorization. Director product commands hold the controller lock, require current unexpired binding and check the actual Host assistant role, exact running tool part, call ID and arguments before dispatch. The tool bridge overwrites inherited caller context. Scope defaults are applied in Runtime instead of modifying Host-recorded tool arguments.

Local evidence: 11 new Primary tests and 17 interaction receipt regressions passed. Host transport is a labeled fixture; these are not real-model or Windows results. Independent reviewer found a Host realm gap in the initial draft; endpoint-bound realm checks and realm-local Session uniqueness repair it. Raw report and recheck remain separate evidence.

Remaining: actual Host proof, all other role fences, update/Gate/control lifecycle, automatic pressure and successor, TUI follow and detached background lifetime. No C1 full PASS or Windows qualification is declared.
