# C1 auxiliary caller increment

1.14.0 CANDIDATE / NOT_QUALIFIED. Knowledge, Requirement recovery and bounded evidence model entry now require actual Host session/message/call ID, exact tool input and the current R1 Mission authority grant. Tool adapters overwrite inherited caller variables. Evidence defaults apply after verification, so omitted optional arguments are not confused with fabricated Host input. Internal trusted import/review services remain separate from the exposed model entry.

Knowledge task/role/project/environment/version scope comes from the current Mission and Attempt. Supplied contradictory task, role or scope rejects. Candidate, review and relation operations retain the original source provenance and local human approval checks; they cannot enable G6 promotion. Requirement recovery is limited to the role already allowed by its native tool contract. Model document imports use `attachments/` or `data/intake/` under the product workspace; explicit trusted local import is a separate entry.

An independent file replacement experiment found that a path check followed by a second open could import outside bytes. The repaired model path uses the established General file component's directory guards without modifying that component. A regular, single-link file is read through a protected descriptor with the original 20 MiB document bound. Parser and attachment cache use the same admitted bytes; the external PDF fallback receives a private snapshot. Reusing a source ID/revision with changed bytes now rejects. POSIX rejects symlinks; Windows holds directories and the final file without delete/write sharing and rejects reparse points.

Local evidence: nine new tests passed independently, including before-open replacement denial and after-read immutable-byte/cache assertions; thirteen existing recovery intake tests passed including DOCX/PDF, secrets, release provenance and approval negatives; the existing >=10 MiB evidence/rotation stress script passed its transport-fixture checks; eleven Primary caller tests passed. See `AUXILIARY_CALLER_RAW_REVIEW.md` and `AUXILIARY_CALLER_COMPONENT_SEAL.json`. No real model or bank execution is claimed.

Prior source f639668de3a2181ddfabe9216c953ad78fb3a79e passed the Windows Mission authority component in run 34700175086: 134 run, 132 pass, two POSIX-only skips; offline Python/HTTP/DB/k6/ZAP job passed. Isolation failed. The new auxiliary increment requires its own subsequent Windows run. Full C1 update/HumanGate/lifecycle/mixed normal-entry closure and C2–G remain open.
