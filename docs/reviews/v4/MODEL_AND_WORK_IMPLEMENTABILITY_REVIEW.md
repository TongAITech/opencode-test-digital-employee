# V4 implementability review

DECISION = PROCEED_WITH_EXPLICIT_VALIDATION_GATES. This permits authorized component construction; it does not qualify the current product or promise universal model capability. Required experiments have been executed or attempted with retained results. Current runtime violations, Windows confinement and final full-model behavior remain unresolved delivery gates.

## Model and execution environment

- REQUESTED_MODEL: GPT-6 Astra; requested reasoning: Ultra.
- OBSERVED_MODEL: `gpt-6-astra`; OBSERVED_REASONING: `ultra`, from this task's actual `turn_context` metadata, recorded with timestamp/source in `MODEL_SETTINGS_EVIDENCE.json`. Model name is not task fitness evidence.
- Actual engineering host: Codex desktop on macOS, with shell/files/Git, independent review agents and actual GitHub Windows runners. The contract calls its environment ChatGPT Work; no cloud Work environment was fabricated.
- Bank runtime model remains the user's host OpenCode/provider path. The real local model experiment used OpenCode 1.18.3 and `opencode/big-pickle`, not Astra substituted into the product. The host configuration digest was unchanged. Host plugin inheritance is not yet proven because the baseline harness disabled default plugins; no claim of full S1 configuration equivalence is made.
- MODEL_TASK_FIT = LIMITED, evidence-supported for the observed source review, code generation, defect reproduction, and one real-host structured seven-task plan. Full business reasoning/execution and bank `aicoder-plus` reliability remain unproven. No representative failure rate, monetary cost or complete token total was available; no estimate is represented as measurement.

## Four independent dimensions

| Dimension | Evidence and classification | Remaining qualification |
|---|---|---|
| WORK_TOOLING | IMPLEMENTABLE_WITH_LOCAL_REAL_INTEGRATION: exact local sources read/hashed, real scratch I/O, atomic Git commits/push, Python compilation/tests, real Chrome, independent workers, actual Windows workflows and artifact retrieval | Windows AppContainer execution is under repair; no-admin parent token is proven, contained child launch is not yet proven |
| ENGINEERING_SUBSTRATE | IMPLEMENTABLE_WITH_LOCAL_REAL_INTEGRATION: sole repository/current source verified, reused offline Windows payload, actual OpenCode/model path, R1/R2/R3/R4 components, pinned browser-use and coverage acquired only after local search | Current source is V4-incomplete; no final V4 package exists; browser-use Windows dependency assembly and full integration remain work |
| MODEL_TASK_FIT | LIMITED: real host model created seven plan tasks through existing entry; independent workers found reproducible runtime/oracle defects | No full V4 same-Mission L4; one planning sample is not a reliability guarantee |
| BANK_FIELD_BOUNDARY | IMPLEMENTATION_READY_BUT_BANK_FIELD_REQUIRED only for genuine bank connectivity, identities, business data and production-like load | No bank credentials/targets used, no bank PASS. Missing local implementation is not relabeled bank-only |

## Mandatory spike evidence

| Spike | Actual result and evidence | What it does not prove |
|---|---|---|
| S0 TOOLS | Exact file read and scratch write/read; existing pressure suite 15 tests passed (`S0-existing-pressure-test.log`). Git read/write/push and new Windows runs/JSON artifacts are real. Old run 34278790485 succeeded only for the old baseline | Old Windows PASS is not V4 qualification |
| S1 HOST | `S1-real-host-baseline/result.json`: real host model structured plan, separate Director/Planner sessions, seven tasks; create/read/message/status/abort exercised; before/after host-config digest unchanged | Full plugin inheritance, all Worker completion, final L4 and bank provider behavior |
| S2 CONTEXT | `reviewer-routing/context-spike/S2_CONTEXT_REPORT.md`: actual OpenCode final serialized fetch; 6 permitted bodies exactly match receiver bytes/hashes, five oversize Chinese/history/tool-argument/tool-result/schema bodies blocked before receiver. Native compact receives injected 400 and stores ContextOverflowError; non-fork successor starts with empty history and sends a small checkpoint | Fixture protocol provider is not real model; 32KiB is an experiment budget, not model capacity. Product automatic recovery/TUI/all roles/token calibration remain NOT_PROVEN. Large generated tool args had already executed before next model request admission; pre-provider checking alone does not undo them |
| S3 ROUTING | `S3_ROUTING_BASELINE_RESULT.json`: 13 direct admission challenges, 12 violate V4; distinct version suffixes collide; General/RuntimeDiagnosis roles absent. Read-root sibling/symlink bypass reproduced in `reviewer-routing/COUNTEREXAMPLES_REPORT.md` | Deliberate wrong proposals do not mean the real model always misroutes. Current GeneralWork actual product I/O is NOT_IMPLEMENTED |
| S4 LIVENESS | `reviewer-architecture/liveness-spike/`: real production controller/R1 + labeled Host HTTP fixture. One-goal three-worker execution observed; fast polling reproduced duplicate dispatch/effects. Idle, busy-pressure rotation, durable-completion/crash, failure and Gate cases expose missing progress/fencing | Host is a fixture, not L4; frequency is bounded to recorded samples. Final S4 report contains 19 valid samples and consistent post-cleanup R1 replay/projection; duplicate effects occurred in 2/2 fast happy samples and 0/1 slower sample. It retains all failed harness attempts |
| S5 ORACLE | `reviewer-routing/COUNTEREXAMPLES_REPORT.md`: four of six DB/CAT ordering negatives falsely pass; real R1 replay retains wrong PASS. Actual local POST plus injected crash/reentry duplicates business write without UNKNOWN_SIDE_EFFECT. Existing positive tests also pass | In-process crash-window injection is not a full OS-kill campaign; explicit reentry does not prove autonomous retry. Fixes and fresh regression remain required |
| S6 BROWSER | Real pinned browser-use 0.13.10/Chrome: text DOM/fill/click and new-engine reattach same live target/sessionStorage pass. Old-engine reuse failures retained. Separate CDP broker prototype has 34 checks, 46 actual denials with no forbidden page/server effect; only two approved effects | Not full G4/Attempt/origin/redirect/HumanGate integration. Same-user raw-CDP discovery is not OS-confined. Model bridge and Windows engine remain unproven |
| S7 TEST TOOLS | Native real pytest with independent mutant failure, coverage 7.13.1 (small synthetic function 100%, not product coverage), httpx 0.28.1, CAT HTTP/401 fixture, SQLite commit/rollback/restart/constraint pass. Actual Windows k6 executes 60 requests at two VUs in a three-second sample. Corrected Windows run 34506247751 executes real ZAP 2.17.0 active scanner 40012 and detects the synthetic reflected XSS; 50 reused plugin hashes remain unchanged and no runtime plugin is installed | Earlier ZAP run unexpectedly downloaded add-on updates because wrong configuration keys were used; it is invalid offline evidence. The corrected silent-mode scan passes its bounded detection oracle; it does not cover complete business security. No vendor DB or bank load claim |

All spikes use synthetic disposable targets. No bank sources, tokens, credentials or mutable runtime stores are committed or published. Browser and HTTP experiments accessed owned local services. Official-source fallback artifacts have exact version/hash evidence. The accidental ZAP auto-update is retained as a failed experiment, not silently reused as pinned input.

## Architecture and construction admission

ARCHITECTURE_CHANGE_REQUIRED = NO_GLOBAL_INVARIANT_CHANGE_DEMONSTRATED; COMPONENT_CONTRACT_CHANGES_REQUIRED = YES. `ARCHITECTURE_AND_COMPONENT_CONTRACT_DECISION.md` defines typed non-Mission R1 roots, extension applicability/compatibility, HostUserTurn admission, scoped leases, OS/broker boundaries, shared scheduling, final-request governance, side-effect/oracle receipts and existing R4-based G6. Independent CC-01 review requested six revisions; they are incorporated with explicit compatibility tests. No formal external frozen registry was found; verification is limited to available v7 governance and actual source.

CODE_IMPLEMENTATION_FEASIBILITY = IMPLEMENTABLE_WITH_LOCAL_REAL_INTEGRATION for authorized component work. LOCAL_QUALIFICATION_FEASIBILITY = PARTIAL; Windows confinement and full L4 need further engineering. No demonstrated need to abandon sole R1, G4 authority or expand bank permissions justifies stopping all work at a Product Owner gate. Unsupported OS execution remains fail-closed while independent construction proceeds.

Proceed through the complete `FINAL_IMPLEMENTATION_CONTRACT_V4.md`. Product source changes must be tested against the reproduced negative oracles, not against weakened expectations. Current P0 violations prohibit final qualification. READY_FOR_BANK_FIELD_VALIDATION = NO; FINAL_V4_ZIP = NOT_CREATED.
