# C1 Mission control increment

1.14.0 CANDIDATE / NOT_QUALIFIED; v7 unchanged; sole durable truth remains R1. Local component review PASS does not close C1 or qualify autonomous Mission completion.

Actual Host-admitted Mission pause/stop/continue now enter a durable operation owner. A new interaction.control_intent_recorded.v1 event commits atomically with the unchanged original claim event. It fixes original admission cursor, Mission control generation, source/target status and no-effect decision. Older owner compositions reject this unknown event before writes/rebuild; no core event is redefined. No-effect recovery cannot become a fresh transition. State changes use canonical PAUSE_MISSION/CANCEL_MISSION/CONTINUE_MISSION with durable idempotency. A committed transition is recovered from its command receipt; an opposing newer generation is rejected. Invalid original transitions such as BLOCKED→pause finish as a durable rejection rather than permanently blocking later requests.

Primary admission and physical effect use distinct non-authoritative mutexes. A long execution cannot prevent a pause request from reaching R1. CONTROL_PENDING means the original request was stored; the background control tick drains it before normal supervision. New G4 effects and G21 dispatch/wake paths respect pending pause/stop. An already admitted G4 call drains before the Mission state changes. Paused/blocked Sessions and in-flight receipts are retained; terminal cleanup remains existing governance. Query reads paused and terminal Missions and exposes pending control references without creating a Mission.

Validation: 16 local R1/Host-fixture tests passed, including actual product boundary under a held effect lock, >5-second effect, duplicate controllers, crash between state and receipt, stale/no-op replay, invalid transition recovery and pending-page filtering. Independent reviewer reproduced and then closed four P1 findings; preserved failing runs are in the external post-c1-review directory. G4 terminal prewrite, explicit HumanGate and G21 Session Router scripts passed. Background Gate fixture was adapted to carry its actual Runtime dependency; its final regression result is recorded separately. The earlier pytest invocation collected no tests because those legacy scripts use main(); it is not PASS evidence.

Remaining C1: other role actual Host/session/epoch/lease fences, Mission update with Planner-owned PlanRevision, HumanGate full typed entry, detach/stop-runtime lifecycle and full mixed-intent real-model integration. C2 Director pressure/automatic successor/TUI and C3 autonomous diagnosis/effect reconciliation remain OPEN. General file/R1 evidence is preserved. Model probe is pending explicit external-service/payload approval after automatic review rejected its launch. No model probe result is fabricated.
