# 10.REC.3 — Actual hosted intake and bounded Session continuation

The authorized starting point is `69a568dc5ed0e4af441569fb039d93fb392e3194`. This repair preserves v7, R1 Event Stream truth, the frozen G1–G5 responsibilities, and G6 HOLD. Runtime-owned planning, scheduling and routing remain in place.

Actual OpenCode tool execution exposed an intake defect that direct service tests missed: `product_entry.orchestration_command` passed the G2.1 provisioning wrapper to the hosted user-message reader. The wrapper has no HTTP message API. Hosted provenance now reads through the underlying directory-scoped transport, while all mutations still use G2.1 and R1.

Continuous real-transport stress exposed a second defect. OpenCode `parentID` is a physical deletion relationship: [pinned 1.18.3 Session removal](https://github.com/anomalyco/opencode/blob/v1.18.3/packages/opencode/src/session/session.ts#L598-L628) recursively deletes children. A rotation successor created as the predecessor's child was therefore deleted during predecessor cleanup. Rotation provisioning omits this physical parent relationship. R1 retains predecessor/successor links, Mission, Task, Logical Agent and root Attempt lineage; no business state moves into OpenCode metadata.

The hosted qualification begins with only the exact text `测试 BLOAN1.9.4`, without specifying an agent. It observes real OpenCode tool events and correlates them with R1 Mission intake, a separate Planner Session, a frozen explicit DAG, role-routed Worker Sessions, and automatic task outcomes. A deterministic local model protocol chooses the test tool calls. This validates runtime/tool integration; it is expressly not evidence that a real AI model independently formed the semantic plan.

The combined stress uses a source of at least 10 MB and the real `aitest_context` tool, with source pages capped at 4096 bytes and a complete response capped at 16 KiB. Actual OpenCode message observations drive the background Control Loop to at least two pressure rotations. The test requires successors to survive predecessor closure and complete the same task with the same durable lineage; unreachable-Session recovery cannot substitute for pressure rotation. Restart and export/replay evidence use the same Mission. Reported request sizes and tool observations contain no full raw source.

The reader admits only the Router's current bound Session/Attempt. Evidence paths use a deterministic Windows-safe Mission directory, reject traversal and cross-Mission links, scan source content incrementally, and pin pagination to a source hash. Existing Runtime read APIs remain bounded; this tool does not expose arbitrary historical runtime/evidence paths. Built-in unrestricted Read and Bash stay denied.

`MACHINE_VALIDATION_RESULT.json` records the exact executed source and each gate's evidence source. Real-model `AUTONOMOUS_PLAN` remains unproven without an approved model service; `WINDOWS_FULL_QUALIFICATION` cannot be promoted merely because all fixture-based packaged tests pass. No synthetic result is Real BLOAN Bank PASS.
