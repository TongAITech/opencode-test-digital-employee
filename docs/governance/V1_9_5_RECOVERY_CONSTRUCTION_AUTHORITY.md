# V1.12.0 Recovery construction scope

Authority: the current user's 2026-09-08 request authorizes actual construction,
testing, repair, dependency acquisition with LOCAL-FIRST / ONLINE-FALLBACK,
Windows CI, and a derived Windows validation ZIP. The follow-up explicitly asks
for version evolution; product recovery release is 1.12.0, historical behavior
baseline is V1.9.4. This supersedes earlier documentation-only packaging scope
restrictions for this isolated recovery branch. It does not reopen architecture
or authorize main mutation, bank result fabrication, or G6 promotion.

- Repository: TongAITech/opencode-test-digital-employee only.
- Canonical main verified remotely: 58e5e1259cd26846b31ea21a8a87df0bcf071edc.
- Latest remote packaging head at construction: 98868e6bf8a1b1d7c5e262ae6d305755bca35886.
- Branch: work/v1.12.0-recovery-turnkey-validation, new isolated worktree.
- Original local main checkout: 4edd785 with untracked .idea directory; untouched.
- ArchitectureBaseline: v7/FROZEN/UNCHANGED. R1 Event Stream sole durable truth.
- Canonical G1-G5 engineering CLOSED/FROZEN; field validation is independent.
- Legacy aitest.db: migration/reference only, never restored as product truth.
- G6: HOLD. Teaching creates governed candidate/reference assets only.

The referenced ChatGPT conversation has text previews, not mounted attachment
bytes in this desktop environment. The named V1.9.4 base and upgrade archives
were not found among accessible mounts. /mnt/data is absent; Downloads access
was refused by macOS. Accessible prior runtime/build/cache trees were scanned
and reused; no claim is made that missing historical patches were replayed.
CAPABILITY_PARITY_MATRIX separates implemented recovery from historical byte
parity that cannot be proven without those specific bytes.

Payload registry precedes network acquisition. Only absent, invalid or
contract-incompatible local artifacts can use fixed official/trusted upstream
identities, with source and SHA256. Build and bank startup perform no downloads.
Windows CI may acquire the already assembled ZIP from an unpublished draft
release solely to run verification, without publishing a release or changing main.

LOCAL_VALIDATION_PASS, WINDOWS_CI_PASS and BANK_FIELD_VALIDATION_REQUIRED remain
distinct. Contract tests use synthetic requirements and HTTP/OpenCode fixtures;
real runner smoke uses local HTTP/DOM targets. No fixture is bank acceptance.
