建立 `10.REC.3｜V1.12 Install-Start Lifecycle & Autonomous Mission Entry Repair`。

继承当前 V1.12 Recovery Engineering Truth。

Conversation != Project Truth。

Canonical repository =
TongAITech/opencode-test-digital-employee

Canonical main =
58e5e1259cd26846b31ea21a8a87df0bcf071edc

Current Recovery branch =
work/v1.12.0-recovery-turnkey-validation

Authorized starting HEAD =
69a568dc5ed0e4af441569fb039d93fb392e3194

Latest successful Windows qualification =
RUN_ID 34177867024

ArchitectureBaseline =
v7 / FROZEN / UNCHANGED

R1 Event Stream remains sole durable Runtime Truth.
legacy aitest.db as Product Truth = FORBIDDEN.
G1-G5 = CLOSED / FROZEN.
G6 = HOLD.

不得重做已经完成的 V1.12 Recovery Construction。
不得重新施工 G1-G5。
不得修改 canonical main。
本 WorkItem 只修真实行内 Field Finding：
INSTALL / START 生命周期，以及自然语言测试请求进入 G2/G2.1 自治链的问题。

━━━━━━━━━━━━━━━━━━━━
一、真实行内 Finding
━━━━━━━━━━━━━━━━━━━━

用户在 Windows Git Bash 实际运行当前 V1.12 包：

./AITEST.sh

能力自检显示：

Runtime READY
OpenCode READY
Model AUTH_REQUIRED
ControlLoop READY
Browser READY
CodeGraph READY
...

用户选择：

1 开始/继续测试对话

实际结果：

AUTH_REQUIRED：
请先在菜单中配置已批准的行内模型服务

OpenCode 没有启动。

这是不符合产品目标的。

当前代码把：

MODEL_READY

错误地作为：

OPENCODE_PROCESS_START

的前置条件。

必须修复。

另一个现场事实：

用户原有行内 OpenCode 已经有可工作的模型/provider 配置，例如：

~/.config/opencode/opencode.json

其中存在：

wizard-local/aicoder-plus

V1.12 当前 package-owned XDG_CONFIG_HOME / OPENCODE_CONFIG_CONTENT
不能简单导致已有行内 provider/model 配置失效。

必须设计安全、明确、可验证的 host OpenCode configuration reuse / import / binding 机制。

不得偷偷读取或复制 secret。

━━━━━━━━━━━━━━━━━━━━
二、安装与启动必须正式分离
━━━━━━━━━━━━━━━━━━━━

本版本必须恢复并冻结以下用户生命周期：

第一次：

解压搬运包
↓
./INSTALL.sh

INSTALL.sh 只负责安装，不启动 OpenCode。

默认正式安装目标：

/d/PFC/AITest

如目录存在必须安全检测，不得静默覆盖用户 Runtime/Data。

INSTALL.sh 必须完成：

1. Windows Git Bash 检测
2. 离线包完整性/SHA256
3. 安装目录创建
4. runtime 复制
5. portable Python
6. OpenCode
7. Chromium / Playwright
8. CodeGraph / ripgrep
9. API / pytest
10. k6
11. Java / ZAP
12. data/state/log/evidence/import/export 初始化
13. bindings 初始化
14. BUILD_PROVENANCE / OFFLINE_PAYLOAD_REGISTRY 安装
15. install self-check
16. 生成安装身份

安装阶段禁止要求：

Model auth
Starlink auth
4A
CAT
DB
在线 pip/npm/playwright install

安装完成以后，用户长期只使用：

cd /d/PFC/AITest
./AITEST.sh

搬运 ZIP 不再是正式 Runtime location。

━━━━━━━━━━━━━━━━━━━━
三、AITEST.sh 启动语义
━━━━━━━━━━━━━━━━━━━━

AITEST.sh 是日常唯一入口。

启动顺序必须改为：

Installed Runtime integrity
↓
Runtime self-check
↓
resolve package-owned workspace
↓
resolve existing approved OpenCode provider/model configuration
↓
启动 package-owned OpenCode process
↓
启动 G2.1 Control Loop
↓
OpenCode PROCESS_READY
↓
随后检测：
AUTH_READY
PROVIDER_MODEL_READY
LLM_RUNTIME_READY
R2_SESSION_READY
↓
满足后 Mission 自治执行

必须保持：

OPENCODE_PROCESS_READY
!=
MODEL_READY

MODEL AUTH_REQUIRED 绝对不能阻塞 OpenCode process 启动。

即使：

Model = AUTH_REQUIRED

也必须：

OpenCode = RUNNING
ControlLoop = RUNNING

用户必须能够进入 OpenCode。

如果需要认证或模型绑定，在 OpenCode 内通过清晰 HumanGate / setup guidance 完成。

不得出现：

选择 1
→ AUTH_REQUIRED
→ 返回菜单
→ OpenCode 未启动

━━━━━━━━━━━━━━━━━━━━
四、宿主 OpenCode 配置复用
━━━━━━━━━━━━━━━━━━━━

先侦察 OpenCode 1.18.3/当前银行实际版本支持的 provider/config resolution。

设计受治理的：

HOST_OPENCODE_CONFIG_DISCOVERY

例如发现：

~/.config/opencode/opencode.json

但必须做到：

1. 不持久化 secret
2. 不复制 API key/password/token
3. 不覆盖用户 host config
4. package-owned runtime 仍保持 isolation
5. 可明确选择：
   REUSE_APPROVED_HOST_PROVIDER
   PACKAGE_LOCAL_PROVIDER_BINDING
   AUTH_REQUIRED
6. host 已存在有效 provider/model 时应尽量复用，不要求用户重新理解 provider 配置
7. 如果 host provider 是自定义插件而非 OpenAI-compatible endpoint，必须支持该真实模式，不得强制用户转换成不存在的 /v1 endpoint

需要真实验证：

wizard-local/aicoder-plus 类配置是否可以被 package-owned OpenCode 安全复用。

如果无法在 CI 证明，明确形成：
BANK_PROVIDER_BINDING_REQUIRED
但启动 OpenCode仍不得被阻塞。

━━━━━━━━━━━━━━━━━━━━
五、自然语言“测试 BLOAN1.9.4”必须真正进入自治链
━━━━━━━━━━━━━━━━━━━━

修完启动后，必须验证：

用户进入 OpenCode 后，只输入：

测试 BLOAN1.9.4

唯一 primary entry =
aitest-director

Director 必须自动调用：

aitest_director
action = start_test
payload.user_request =
"测试 BLOAN1.9.4"

不得要求用户手工执行：

/aitest-plan
/aitest-scheduler
/aitest-execute
创建 Session
切换 Agent
rotate session

完整链必须是：

User Turn
↓
aitest-director
↓
R2.2 Mission / Goal durable intake
↓
R1 Event Stream
↓
G2.1 创建 Planner Logical Agent
↓
Session Router 创建真实 Planner Session
↓
aitest-planner 自主形成 semantic Plan
↓
R2.3 validate/freeze
↓
Scheduler auto advance
↓
Session Router 根据 Task Role/Capability 选择 Logical Agent
↓
创建 Worker Session
↓
Requirement Analyst / Code Analyst /
Test Strategist / Case Designer / Executor /
Evaluator / Diagnosis
↓
Task outcome
↓
Scheduler 自动调度下一个 Ready Task
↓
直到 Testing Goal 收敛

必须证明不是 Director 在一个 Session 内假扮所有 Agent。

━━━━━━━━━━━━━━━━━━━━
六、BLOAN Planning Task 粒度
━━━━━━━━━━━━━━━━━━━━

Planner 必须按 durable 小任务拆分，避免再次形成 V1.9.4 巨型 Session。

至少应支持类似：

Current Release Truth
Requirement/SST Intake
Requirement Analysis
BR Analysis
SR Analysis
TR Analysis
BR/SR/TR Consistency
Code Revision Truth
Code Impact
API Impact
UI/Page Impact
DB Impact
Risk Analysis
L1-L7 Scope
Standard Case Design
API Execution
UI Execution
Unit Execution
DB/CAT
Security
Performance
Evidence Evaluation
Coverage Gap
Defect Investigation
Regression / Convergence

具体 Task 由 AI Planner 根据证据自主决定，
不得硬编码固定 BLOAN Plan。

但必须保证：

Task 有 stable task_key
Task 有 intent
Task 有 acceptance criteria
Dependencies 显式 DAG
Task 足够小以支持 Session/Attempt isolation

━━━━━━━━━━━━━━━━━━━━
七、Session Router / Control Loop 必须真实工作
━━━━━━━━━━━━━━━━━━━━

当前 G2.1 能力不得回退。

必须保留并验证：

Logical Agent != Session

Session Router owns:
WHO / WHERE

Scheduler owns:
WHEN / READY

Planner owns:
WHAT

Control Loop owns:
Session observation / rotation / recovery

必须验证：

context >= threshold
或
message/turn/activity/compaction/fallback pressure

触发：

checkpoint
→ RotationRequest
→ successor Session
→ bounded ContextPack
→ same Mission
→ same Task
→ same Logical Agent
→ same root Attempt lineage
→ predecessor close

Agent 不得自己管理 Session 生命周期。

━━━━━━━━━━━━━━━━━━━━
八、V1.9.4 Context Explosion 回归必须纳入验收
━━━━━━━━━━━━━━━━━━━━

历史现场已确认：

大文件如：

runtime-events.jsonl ~19MB
response-observations.json ~10.5MB
events.jsonl ~11MB
network capture MB-level

曾可被 Read/cat 原样送入模型，
导致数百万 token 级上下文输入和 AI_APICallError。

V1.12 必须验证：

RAW_LARGE_RUNTIME_FILE_DIRECT_CONTEXT_INJECTION =
FORBIDDEN

Agent 不允许直接 unrestricted Read/cat 大 Runtime/Evidence 文件。

必须走：

bounded reader
pagination
summary/index
durable fact reference
evidence reference

而不是原文进入 Session。

至少新增一个 Context Stress validation：

构造 >=10MB Runtime/Evidence source
↓
Agent/Tool 不得把全文送模型
↓
bounded read
↓
Control Loop pressure detection
↓
至少发生 2 次自动 Session Rotation
↓
Mission/Task/LogicalAgent lineage 不丢失
↓
Task 最终完成

验收：

CONTEXT_TOO_LARGE_ERROR = 0
AI_APICallError_CONTEXT_OVERFLOW = 0

注意：
synthetic stress PASS 仍不是 Real BLOAN Bank PASS，
但必须作为 package qualification。

━━━━━━━━━━━━━━━━━━━━
九、Windows Qualification
━━━━━━━━━━━━━━━━━━━━

修复后必须重新生成完整 Windows x64 turnkey package。

必须重新执行：

Recovery turnkey Windows validation

不能复用 Run 34177867024 作为新修复的资格证明。

必须验证：

INSTALL.sh
→ /d/PFC/AITest

然后从安装目录：

./AITEST.sh

验证：

OpenCode process starts even when Model AUTH_REQUIRED
Control Loop starts
doctor PASS
host model/provider discovery
natural-language Director intake
Mission creation
Planner Session
Plan freeze
Scheduler auto advance
Router agent selection
Worker Session
automatic rotation
resume
evidence export
restart recovery

所有测试必须使用 packaged Python / packaged runtime。

━━━━━━━━━━━━━━━━━━━━
十、最终交付
━━━━━━━━━━━━━━━━━━━━

必须形成：

INSTALL.sh

/d/PFC/AITest/AITEST.sh

INSTALL_MANIFEST.json
OFFLINE_PAYLOAD_REGISTRY.json
BUILD_PROVENANCE.json
MACHINE_VALIDATION_RESULT.json
CAPABILITY_PARITY_MATRIX.md
VALIDATION_README.md

最终 ZIP：

AITest-V1.12.0-Recovery-Turnkey-Windows-x64.zip
或明确的新 patch/minor identity

必须给出：

FINAL_BRANCH
FINAL_HEAD
WINDOWS_RUN_ID
WINDOWS_JOB_ID
FINAL_ZIP
FINAL_ZIP_SHA256

只有以下全部 PASS 才允许 Closure：

INSTALL_START_LIFECYCLE = PASS
OPENCODE_START_WITH_MODEL_AUTH_PENDING = PASS
CONTROL_LOOP_START = PASS
HOST_PROVIDER_DISCOVERY = PASS/PARTIAL_BANK_BINDING
NATURAL_LANGUAGE_START_TEST = PASS
MISSION_INTAKE = PASS
PLANNER_SESSION = PASS
AUTONOMOUS_PLAN = PASS
SCHEDULER_AUTO_ADVANCE = PASS
SESSION_ROUTER = PASS
AUTO_ROTATION = PASS
SUCCESSOR_RESUME = PASS
CONTEXT_STRESS = PASS
WINDOWS_FULL_QUALIFICATION = PASS
FINAL_ZIP_SEALED = PASS

真实行内：

BLOAN
Starlink
4A
CAT
DB
增量覆盖平台

仍允许标：

BANK_FIELD_VALIDATION_REQUIRED

不得冒充银行 PASS。

执行模式：

Repository Reality Recon
→ narrow design
→ implementation
→ focused tests
→ regression
→ Windows install/start qualification
→ autonomous mission qualification
→ context stress qualification
→ final ZIP
→ final evidence

不要停在设计。
自主修复直到所有非银行 Gate PASS。